import { View, Text } from "react-native";

const SearchBar = () => {
  return (
    <View style={{ flexDirection: "row" }}>
      <Text>SEARCH BAR TEMP</Text>
    </View>
  );
};

export default SearchBar;
